import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "purple",
  },
  
  text: {
    fontSize: 20,
    fontWeight: "bold",
    color: "white",
  },

});

export { styles };
